{
  "name": "recipe-app-root",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd recipe-app && npm install && npm run build",
    "dev": "concurrently \"cd recipe-app && npm run dev\" \"cd server && npm run watch\""
  },
  "dependencies": {
    "express": "^4.21.1",
    "graphql": "^16.9.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}

